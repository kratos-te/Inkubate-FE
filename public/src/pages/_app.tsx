import "@/styles/globals.scss";

import type { AppProps } from "next/app";

const MarketplaceApp = ({ Component, pageProps }: AppProps) => (
  <Component {...pageProps} />
);

export default MarketplaceApp;
